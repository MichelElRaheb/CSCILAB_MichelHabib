#include <stdio.h>
#include <math.h>
# define coffee_beans 8
# define water 30
# define waterM 39
# define milkC 70
# define milkM 160
# define CS 30
# define ADpass 1235

//Minimum ingrdients before alerted 
# define CBmin 10
# define watermin 50
# define milkmin 200
# define CSmin 40

//firt menu varibles
#define customer 1
#define admin 2 
#define exit 3

float summoney(float , float , float , float , float ,float ,float , float, float ,float , float );
float checkmoney(float money);
int refill(int a);
int refillW(int );
int refillm(int );
#define a 1 
#define b 2 
#define c 3

int main()

{ 
    float requiredmoney, sum, counter;
    int choice;
    int coffeechoice; 

    {
        puts("welcome to coffee");
        printf("1.customer\n 2.admin\n");


        scanf("%d", &choice);
        switch (choice)
        {
        case 1:
        puts("mocha :     5.5");
        puts("cuppacino :     4.5");
        puts("espresso :     3.5");
        scanf("%d", &coffeechoice);
        if (coffeechoice == 1) { 
        printf("making mocha ");
        }
        else if (coffeechoice == 2){
        printf("making cuppacino");
        }
        else {
            puts("making esspresso");
        }
        break;
        case 2 :
        puts("admin page");
        break;
            default: 'C';
        break;
        }


    }
}
float sum(float , float , float , float , float ,float ,float , float, float ,float , float )
counter = 0;
while (sum < requiredmoney) {
    sum += counter;
    counter++;
}

float checkmoney(float money)
{
    scanf("%d", &sum);
    if (sum == requiredmoney){
    puts("Thankyou! Preparing your drink.");
    }
    else {
        puts("Pleas use 1 or 0.5 aed coins");
    }
}

int refill(int a)
{
    if (coffee_beans <= CBmin);
    printf("Refill coffee beans.");
    else;
    return 0;
}
int refillW(int )
{ 
    if (waterM <= watermin);
    printf("Refill water.");
    else;
    return 0;
}
int refillm(int )
{
    if (milkM <= milkmin);
    printf("Refill milk.");
    else;
    return 0;
}
